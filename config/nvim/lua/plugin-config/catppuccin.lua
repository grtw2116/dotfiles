require("catppuccin").setup({
	integrations = {
		flash = true,
		aerial = true,
		diffview = true,
		dropbar = {
			enabled = true,
		},
		noice = true,
		notify = true,
	},
	transparent_background = true,
})
