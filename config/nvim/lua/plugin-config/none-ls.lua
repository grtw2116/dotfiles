local null_ls = require("null-ls")

null_ls.setup({
	-- debug = true,
	-- sources = {
	-- 	null_ls.builtins.diagnostics.textlint.with({
	-- 		extra_filetypes = { "tex", "plaintex" },
	-- 	}),
	-- },
})
